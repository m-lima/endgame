# EndGame

## Options

* Whitelist source auth bypass (e.g. 127.0.0.1)
* OpenIDC configuration
* API access (set a header to activate the flow. The header is not CORS allowed)
* User allow list per location
